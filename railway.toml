[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node scripts/migrate.js && ./node_modules/.bin/next start -p ${PORT:-3000} -H 0.0.0.0"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
